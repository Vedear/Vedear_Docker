#!/bin/bash

# Генератор случайного пароля длиной 8 символов
echo "=== ГЕНЕРАТОР СЛУЧАЙНОГО ПАРОЛЯ ==="

# Генерируем пароль из 8 случайных символов, используя: буквы A-Z, a-z, цифры 0-9
PASSWORD=$(cat /dev/urandom | tr -dc 'A-Za-z0-9' | head -c8)

echo "Ваш новый пароль: $PASSWORD"

read